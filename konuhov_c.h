#include <stdio.h>

#define in_this_particular_case if
#define otherwise else
#define so_on_and_so_forth(letter, num) for(int letter = 0; letter < num; letter++)
#define tutorial int main()
#define so_now return
#define lets_say printf
#define let(type, name) type name

